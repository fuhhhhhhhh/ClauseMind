# API Notes

Initial implemented endpoints:

- `GET /health`
- `GET /api/v1/system/config`

The remaining route skeletons are present under `/api/v1` and return placeholder payloads until their MVP phase is implemented.
